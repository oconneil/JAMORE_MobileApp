# JavaScript is the learner's starting point

The learner has written JavaScript and wants to transfer that mental model to reading function behavior in Flutter/Dart, beginning with the application's login path. Future lessons can assume familiarity with callbacks, promises, `async`/`await`, objects, and exceptions, while checking the exact depth through exercises.
